#pragma once

#include <vector>
#include <memory>
#include <string_view>

// TODO: implement

class Profile
{
	// Needs to contain all the settings
public:

private:
	//std::vector<std::string_view, std::unique_ptr<ValueContainer>> valueMap;
};
